@model IEnumerable<Nebula.Domain.Entities.QuizSkeleton>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>გამოცდის ჩონჩხი</h2>

<p>
    @Html.ActionLink("ახლის შექმნა", "Create", new { id = ViewBag.Id })
</p>
<table class="table">
    <tr>
        <th>
     ტიპი
        </th>
        <th>
           მოდული
        </th>
        <th>
         კითხვების რაოდენობა
        </th>
        <th>
      ნუმერაცია
        </th>
        <th>
           პირობა? 
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CSQuestionCategory.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Module.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QuestionCount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ordering)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsTask)
        </td>
        <td>
            @Html.ActionLink("რედაქტირება", "Edit", new { id=item.Id }) |
            @Html.ActionLink("წაშლა", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
