

@if (Session["Teacher"] != null)
{
    using (Html.BeginForm("LogOff", "Teacher", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right">
            <li>
              
                @Html.ActionLink("გაარჯობა " + Session["Teacher"] + "!", "Manage", "Teacher", routeValues: null, htmlAttributes: new { title = "Manage" })
            </li>
            <li><a href="javascript:document.getElementById('logoutForm').submit()">გამოსვლა</a></li>
        </ul>
    }
}

